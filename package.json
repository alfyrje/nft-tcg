{
  "name": "tcg-nft-game",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "(cd hardhat && npm install) && (cd backend && npm install) && (cd frontend && npm install)",
    "start-backend": "cd backend && npm run start",
    "start-frontend": "cd frontend && npm run dev",
    "deploy": "cd hardhat && npx hardhat run --network amoy scripts/deploy.js"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.2.3",
    "ethers": "^6.4.0",
    "hardhat": "^2.22.5",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0"
  }
}
