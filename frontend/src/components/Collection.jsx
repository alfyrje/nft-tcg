import React from 'react';
export default function Collection({address}){
    return (<div>
        <h2>Your Collection</h2>
        <p>In the full app this will query the CardNFT contract for owned tokens and show them.</p>
    </div>);
}
